org.gradle.daemon=false
org.gradle.jvmargs=-Xms512M -Xmx1G -Dfile.encoding=UTF-8 -Dconsole.encoding=UTF-8
org.gradle.logging.level=quiet

gdxVersion=1.14.0
lwjgl3Version=3.4.1
imguiVersion=1.92.7.1
projectVersion=1.0.3

# Single source of truth for build.gradle's appName (used for eclipse/IDE project naming)
# and for the packaged app's own identity (.github/workflows/package.yml, BUILD.md) — change
# these here and every generated project name / installer / desktop shortcut picks it up,
# nothing else needs editing.
appName=hud-creator-gdx
appVendor=Eliezer Dev
appMenuGroup=Games
